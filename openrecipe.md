# Open Recipes Exploration

Open Recipes is a YAML standard for storing recipes, It contains 4 required root keys being:

- `recipe_name`
- `yeilds`
- `ingredients`
- `steps`

and some optional root keys are:

- `notes`
